import React from 'react';import { resolveImage } from '../utils';export default function ProductCard({p}){return(<div className='card p-3'><img className='img' src={resolveImage(p.imageUrl)} alt={p.name}/><div className='mt-2 font-semibold'>{p.name}</div><div className='text-sm text-zinc-500'>{p.category} · {p.subcategory}</div><div className='mt-1 font-bold'>$ {p.price}</div></div>) }