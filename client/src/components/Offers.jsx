import React from 'react';import { resolveImage } from '../utils';export default function Offers({items}){ if(!items?.length) return null; return(<div className='container my-4'><div className='grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4'>{items.map(o=>(<div key={o._id||o.id} className='card'><img className='w-full h-40 object-cover rounded-2xl' src={resolveImage(o.imageUrl)} alt={o.title}/><div className='p-3'><div className='font-semibold'>{o.title}</div><span className='badge mt-1 inline-block'>{o.tag}</span></div></div>))}</div></div>) }